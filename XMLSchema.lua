---@class XMLSchema
XMLSchema = {};

---Creates a new XML schema, used to register and later validate/read custom XML paths and attributes for a config file.
---@param name string Name of the schema, typically matching the root element of the XML files that use it
---@return XMLSchema
function XMLSchema.new(name)
end
